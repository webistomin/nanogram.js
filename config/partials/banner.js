import * as pkg from '../../package.json';

export const BANNER = `/**
*
* ${pkg.name}
*
* @version ${pkg.version}
* @author ${pkg.author.name}
* @email: ${pkg.author.email}
* @license: ${pkg.license}
*
**/\n\n`;
